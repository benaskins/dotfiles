# ~/.aliases — muscle memory preserved

# Git aliases
alias gs="git status"
alias gd="git diff HEAD"
alias gc="git commit -m"
alias gca="git commit -a -m"
alias gu="git reset --soft HEAD^"
alias gra="git status | grep deleted | awk '{print $3}' | xargs git rm"
alias gp="git push"

# Navigation aliases
alias ll="exa -la"
alias ..="cd .."
alias ...="cd ../.."
alias mkdir="mkdir -p"

# System aliases
alias df="df -h"
alias du="du -h"
alias free="free -h"

# Python aliases
alias py="python"
alias pip="pip"
alias venv="python -m venv"
alias activate="source .venv/bin/activate"
alias deactivate="deactivate"
alias pip-upgrade="pip list --outdated | cut -d ' ' -f1 | tail -n +3 | xargs -n1 pip install -U"

# Utility aliases
alias reload="source ~/.zshrc"
alias path='echo $PATH | tr -s ":" "\n"'
alias ip="curl -s https://api.ipify.org"
alias localip="ipconfig getifaddr en0"
